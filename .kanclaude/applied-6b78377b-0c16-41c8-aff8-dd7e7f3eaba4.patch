diff --git a/src/ui/logo.rs b/src/ui/logo.rs
index cbb9226..b9f4137 100644
--- a/src/ui/logo.rs
+++ b/src/ui/logo.rs
@@ -35,6 +35,8 @@ pub enum EyeAnimation {
     Dizzy,
     /// Sleepy - half-closed eyes (˘ ˘)
     Sleepy,
+    /// KB - branded eyes (K B)
+    KB,
 }
 
 /// Animated star eye frames for celebratory animations
@@ -57,6 +59,7 @@ impl EyeAnimation {
             EyeAnimation::Heart => ("♥", "♥"),
             EyeAnimation::Dizzy => ("@", "@"),
             EyeAnimation::Sleepy => ("˘", "˘"),
+            EyeAnimation::KB => ("K", "B"),
         }
     }
 
@@ -82,7 +85,7 @@ impl EyeAnimation {
         let hash = nanos
             .wrapping_mul(0x517cc1b727220a95)
             .wrapping_add(0x9e3779b97f4a7c15);
-        let index = (hash >> 32) as usize % 10;
+        let index = (hash >> 32) as usize % 11;
 
         match index {
             0 => EyeAnimation::Blink,
@@ -94,7 +97,8 @@ impl EyeAnimation {
             6 => EyeAnimation::Wide,
             7 => EyeAnimation::Heart,
             8 => EyeAnimation::Dizzy,
-            _ => EyeAnimation::Sleepy,
+            9 => EyeAnimation::Sleepy,
+            _ => EyeAnimation::KB,
         }
     }
 }
